# App

This folder contains the FastAPI backend and React frontend for the project. Full project documentation and run instructions are in the repository root `README.md`.

Run commands for the app are in `app/backend/requirements.txt` and the root `README.md`.

